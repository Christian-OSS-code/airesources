# Frontend - AI Resources Management

This is the React-based frontend for the AI Resources Management application, built with Vite.

## 🚀 Getting Started

1.  **Install Dependencies**:
    ```bash
    npm install
    ```

2.  **Run Development Server**:
    ```bash
    npm run dev
    ```

3.  **Build for Production**:
    ```bash
    npm run build
    ```

## 🏗️ Architecture

*   **Vite**: Frontend tooling and dev server.
*   **React**: UI library.
*   **Vanilla CSS**: Used for styling all components.
*   **API Integration**: Located in `src/api.js`.

For a full project overview, see the [Root README](../README.md).
